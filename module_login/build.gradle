if (ProjectBuildConfig.isAppMode) {
    apply plugin: "com.android.application"
} else {
    apply plugin: "com.android.library"
}
apply plugin: "org.jetbrains.kotlin.android"

apply from: "${rootProject.rootDir}/base_module.gradle"

